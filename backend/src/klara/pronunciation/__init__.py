"""Pronunciation assessment via Azure AI Speech.

Code adapted from sssamuelll/speechtotext (`api/azure_client.py`,
`api/schemas.py`, `core/audio.py`). Kept as a self-contained subpackage
so swapping the provider later (e.g. open-source) only touches files in
here plus the router.
"""
