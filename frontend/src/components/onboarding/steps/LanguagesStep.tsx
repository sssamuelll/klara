import Step, { type StepProps } from "../Step";

export default function LanguagesStep(props: Omit<StepProps, "title" | "children">) {
  return <Step {...props} title="(stub LanguagesStep)">stub</Step>;
}
